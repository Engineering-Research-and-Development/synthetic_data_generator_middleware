name: Python Lint and Security Middleware

on:
  push:
    branches: [ "middleware" ]

jobs:
  lint_tools:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12"]

    name: Linting
    steps:
      - name: Check out source repository
        uses: actions/checkout@v5

      - name: Set up Python environment ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r src/model_registry/test/requirements.txt
          pip install ruff bandit

      - name: Run Ruff
        run: |
          cd src/model_registry/server
          python -m ruff check .
          python -m ruff format --check .

      - name: Run bandit
        run: |
          cd src/model_registry          
          python -m bandit -c bandit.yaml b-r .